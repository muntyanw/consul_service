{
    "name": "Run e-Consul bot",
    "type": "python",
    "request": "launch",

    "program": "${workspaceFolder}/manager.py",

    "cwd": "${workspaceFolder}",       
    "env": {
        "PYTHONPATH": "${workspaceFolder}" 
    }
}
